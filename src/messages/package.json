{
  "name": "sf-pdp-messages",
  "private": true,
  "version": "0.2.0-alpha.0",
  "main": "index.js",
  "types": "index.d.ts",
  "author": "SIL",
  "license": "MIT",
  "scripts": {
    "build": "npm run build:types && npm run build:functions",
    "build:types": "tsc --emitDeclarationOnly",
    "build:functions": "cross-env NODE_ENV=production npx esbuild index.ts --bundle --minify --platform=node --outfile=dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.6",
    "typescript": "^5.8.3"
  }
}
